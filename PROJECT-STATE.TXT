Images and audio are correct up to scene 127 (Introduction and Chapter 1 ready).

Images are missing from scene 128 onwards.

I used whisperx and identified where each slide syncs with the text and updated the data.js

However, the audio is missing scenes 190-203. 

However, given that scenes are missing we need to fix that first. 
The scene text is correct but we miss the audio from scene 190 to 203 and need to re-create it with eleven labs and the voice we used the first time.

Then glue the new part to the end of chapter2 and it will be ready.


Scene 204 is Chapter 3 and the audio is fine onwards.


Syncing happens with: 
$ node scripts/alignment/align-scenes.js --transcript .alignment/TheChroniclesOfGatan-Audio.json --apply

Not sure if we can re-use the align-scenes.js as is.
